---
# hardening.yml — Apply Linux hardening only.
# Use --tags ssh, firewall, auditd, password, or services to scope the run.

- name: Harden Linux hosts
  hosts: linux
  gather_facts: true
  roles:
    - role: linux_hardening
      tags: [hardening]
